<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <%- include(__public + '/head.ejs'); %>
</head>
<body>
<form class="uk-light" method="post" action="/login"
      style="width: 320px; text-align: center; position: absolute; transform: translate(-50%, -50%); top: 50%; left: 50%">
    <h1 style="font-size: 60px; margin: 0"><b>YariHub</b></h1>
    <% if (__query.hasErr) { %>
        <div class="uk-alert-danger" uk-alert>
            <a class="uk-alert-close" uk-close></a>
            <p><%= __query.err === "noaccess" ? "The Access-Key is not valid or wrong!" : "" %></p>
        </div>
    <% } %>
    <div class="uk-margin" uk-margin style="width: 100%">
        <div uk-form-custom="target: true">
            <input class="uk-input" type="password" name="accessKey" placeholder="Enter Access-Key here">
        </div>
        <button class="uk-button uk-button-default">Access</button>
    </div>
</form>
</body>
</html>
